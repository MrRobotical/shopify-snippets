{% comment %}
To be placed just below the <div class="accordion__content rte" id="ProductAccordion-{{ block.id }}-{{ section.id }}"> on the main-product.liquid file
{% endcomment %}
<div id="variant-details">{{ product.selected_or_first_available_variant.metafields.custom.variant_details }}</div


{% comment %}
To be placed in the main-product.liqud file. Just above the {%schema%} tag
{% comment %}
<script id="variant_metafield_data" type="application">
{
  {% for variant in product.variants %}
    {% assign metafield_content = variant.metafields.custom.variant_details %}
    "{{ variant.id }}":"{{ metafield_content }}"{% unless forloop.last %}, {% endunless %}
  {% endfor %}
}
</script>
